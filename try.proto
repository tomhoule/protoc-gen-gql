syntax = "proto3";

// Describes a Pizza topping
message Topping {
    string name = 1;
    bool sweet = 2;
}

// A pizza, with toppings and stuff
message Pizza {
    string title = 1; // How the pizza is usually called
    // Where the pizza is from
    string origin = 2;
}
